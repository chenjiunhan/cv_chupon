clear
clc
close all

Re = imread('ball_reflectance.png');
S = imread('ball_shading.png');
I = R .* S;
R = input_image(:,:,1);
    G = input_image(:,:,2);
    B = input_image(:,:,3); 
disp(size(R));
disp(size(S));
figure, imshow(R);
figure, imshow(S);
figure, imshow(I);